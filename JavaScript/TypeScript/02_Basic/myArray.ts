const superHeros:string[]=[];
const herowPower:number[]=[];
superHeros.push('Thor');
herowPower.push(9);

const herowPowerNew:Array<string>=[];
herowPowerNew.push('sddf');
//herowPowerNew.push(5);

type User={
    name:string,
    isActive:boolean
}

const allUsers:User[]=[]
//allUsers.push("ssasa");
allUsers.push({name:"",isActive:true})

const MLModel:number[][]=[
    [1,2,3],
    []
]